package com.firstclub.membership.enums;

/** Kinds of benefits a tier can be configured with. Add a new constant to introduce a new benefit type. */
public enum BenefitType {
    FREE_DELIVERY,
    PERCENTAGE_DISCOUNT,
    EARLY_ACCESS,
    PRIORITY_SUPPORT,
    EXCLUSIVE_COUPON
}
